/**
 * UpdateMenuItemDto is defined alongside CreateMenuItemDto in
 * ./create-menu-item.dto.ts (all imports across the codebase resolve there).
 * This file re-exports it so the conventional per-DTO path also works.
 */
export { UpdateMenuItemDto } from './create-menu-item.dto';
